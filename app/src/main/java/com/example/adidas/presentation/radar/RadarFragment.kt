package com.example.adidas.presentation.radar

import androidx.fragment.app.Fragment
import com.example.adidas.R

class RadarFragment: Fragment(R.layout.radar_fragment) {
    companion object {
        fun newInstance() = RadarFragment()
    }
}